{
  "title": "Amazon Core Scraper",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "domains": {
      "title": "Amazon domains",
      "type": "array",
      "items": { "type": "string" },
      "prefill": ["amazon.co.uk"],
      "editor": "stringList",
      "description": "List of Amazon domain URLs to scrape (e.g., amazon.com, amazon.co.uk). Include the full domain."
    }
  },
  "required": ["domains"]
}
